# 作业票系统功能更新

## 更新日期：2026年9月8日

### 客户反馈功能调整

- “上传标准文件”入口从数字驾驶舱迁移至管理中心“法律法规”页面，并按法律法规、规章制度、操作规程分类上传。
- 标准文件改为数据库持久化保存；上传后自动同步更新法律法规资料库、驾驶舱数量、最新文件和安全动态，支持登录后下载。
- 受限空间作业申请中的 JSA 上传区强化为醒目的“JSA 作业安全分析资料”卡片，明确支持图片、Word 和 PDF。
- 关联票证支持用中文逗号、英文逗号、顿号、空格或分号输入多个编号，统一规范为大写并一次关联；查询结果继续使用演示数据，保留输入编号，默认前两张为盲板抽堵票、第三张为临时用电票，并逐张显示状态、人员和审核信息。模拟完成时间以本次受限空间票申请时间为基准，分布在前 1—2 天的上午和下午且互不相同。
- 气体浓度检测记录详情整体恢复旧版卡片结构：顶部统一添加记录，每个检测点仅填写位置、上传现场照片并选择合格/不合格；支持多图压缩、缩略图、删除和全屏预览。

## 更新日期：2026年9月7日

### 应急救援正式闭环与驾驶舱联动

- 新增可控的“事故模拟”测试开关：启用后注入明确标注为测试数据的人员体征异常与气体超限信息，立即触发驾驶舱报警；关闭后恢复实际数据判定。生产环境默认禁用，仅在 `ENABLE_EMERGENCY_SIMULATION=true` 时显示和允许操作。
- 新增应急事件、现场气体读数和应急附件数据表，应急处置进度、交互记录、预案层级、救援方式与时间线均可持久化和追溯。
- 新增应急监测联动接口，对厂家手表实时体征与现场气体检测读数进行联合判断；只有人员异常与气体超限同时成立时才生成驾驶舱突发险情候选报警。
- 驾驶舱新增红色闪动报警窗、声音提示、人员体征和气体超限详情，支持确认启动、填写原因排除和并入已有事故三种处理。
- 应急处置界面重构为流程与历史、现场和无人机视频切换、AI 处置引导、实时气体检测四大区域；未接入的视频流保持明确占位，不伪装为实时数据。
- 处置引导支持按企业实际预案动态展示响应方案，完成现场播报、电子围栏、进入式/非进入式救援判断、事态控制判断及专项/综合预案升级。
- 应急恢复增加现场清理、环境修复、秩序恢复、善后处理和解除管制清单；应急结束和总结评审附件已改为后端持久化上传与下载。
- 左侧菜单中“应急处置管理”已调整为“安全管理体系”分组第一项。
- AI 处置交互改为先显示操作人回复和动态生成状态，约 850-1200 毫秒后再显示并播报系统回复，模拟真实大模型响应节奏。

---

## 更新日期：2026年9月4日

### 驾驶舱智能手表联动

- 新增厂家手表平台服务适配层，通过服务端环境变量读取账号密码并代理设备与健康数据，前端不接触厂家凭据。
- 驾驶舱每 30 秒刷新手表快照，当前只在实时卫星地球上显示“化院1”和“化院2”；在线、离线、告警分别使用绿色、灰色、红色标记。
- 重构手表定位图层，使用锚定球面的地图针替换普通圆点和胶囊标签，点击地图针或设备下拉框均可回到设备位置。
- 修正厂家 `-1,-1` 未定位占位值误判问题；当厂家只返回百度坐标时，在服务端完成 BD-09、GCJ-02 到 WGS84 的转换后再落到卫星地球，不再为避免重叠而人为偏移坐标。
- 地图详情增加六位小数 WGS84 坐标、有效定位数量、最近同步时间和“定位到地图针”操作；20 米内设备仅提示同位置数量，仍各自使用真实坐标。
- 驾驶舱中心地图由低分辨率三维地球切换为 Leaflet 城市级交互地图，支持卫星影像与道路地图切换；卫星模式叠加道路、地名和行政参考图层。
- 增加江阳区、纳溪区、龙马潭区、泸县、合江县、叙永县、古蔺县七个区县的边界轮廓、区县名称和图例，并支持一键显示或隐藏。
- 化院1和化院2相距约3米时使用带设备数量的聚合地图针，点击聚合点可放大并在最大层级展开设备，避免两个地图针相互遮挡。
- 修正卫星影像放大后出现“Map data not yet available”占位瓦片的问题：卫星模式限制到数据源实际覆盖层级并使用可用影像平滑放大，道路模式仍可进入更高层级查看街道细节。
- 点击手表标记可查看心率、血氧、体温、血压、步数、信号和最后通信时间；中心指标同步显示在线手表数量和实时心率。
- 新增登录鉴权保护的 `GET /api/watches/latest` 接口，并对厂家登录及数据请求设置短时缓存和超时，避免驾驶舱频繁直连厂家平台。
- 运行前需在服务端配置 `WATCH_PLATFORM_USERNAME` 和 `WATCH_PLATFORM_PASSWORD`；可选配置 `WATCH_PLATFORM_API_BASE`。后续如需增加设备，可通过逗号分隔的 `WATCH_PLATFORM_DEVICE_NAMES` 调整接入名单。

---

## 更新日期：2026年8月27日—28日

## 对应分支：`feat/2026-08-27-cockpit-permit-upgrade`

- 本次驾驶舱、应急处置和受限空间作业票改造统一保存在该独立分支。
- 原 `feat/zizhi` 分支未追加本次提交，继续保留改造前驾驶舱代码，便于后续对照或恢复。

---

### 一、驾驶舱与应急处置大屏演示改造

- 驾驶舱中心直接接入 GitHub 开源 `react-globe.gl`，使用其官方 Blue Marble 与地形纹理，支持拖拽旋转、滚轮缩放及泸州定位；核心指标位于地球下方。
- 地球默认定位泸州市并显示红色标记；缩放标注按中国、四川省、泸州市三级切换，最大仅放大至城市级。
- 三级中文地名改为浏览器原生 HTML 地图标注，避免 3D 默认字体缺少中文字形而显示问号；泸州使用红色地图针样式。
- 增加按缩放层级切换的地名图层：中国层显示全国省级名称，四川层显示全部地级市与自治州，城市层只保留泸州市红色定位。
- 四川层移除城市白色点位；悬浮城市名称时显示对应市州轮廓，触屏设备可点击城市名称保持或取消轮廓。
- 四川市州 GeoJSON 已保存到前端本地静态资源，地图交互不依赖运行时公网行政区接口。
- 行政区改为透明面、按需显示轮廓模式：平时不绘制市界，悬浮城市名称时只显示对应城市的青蓝色轮廓线，不使用红色高亮或面填充。
- 四川层仅将当前悬浮或点击的单个市州边界交给 WebGL 渲染，避免 21 个高精度 MultiPolygon 同时建模造成缩放卡顿；城市文字节点保持稳定，避免悬浮重建闪烁。
- 市州高亮由透明多边形描边改为独立路径层：GeoJSON 环直接转换为无填充发光线段，避开球面多边形方向与透明材质兼容问题。
- 重新校准三级镜头阈值：默认泸州层进入真实城市近景，向外缩放一级显示四川市州，再向外显示全国省名，避免市州名称在亚洲视野中堆叠导致悬浮命中错误。
- 移除在部分浏览器中出现影像解码错误和黑屏的 Cesium 运行链路，避免公网图层异常造成驾驶舱不可用。
- 法律法规区域增加图片、Word、PDF 模拟上传入口；上传文件名会进入安全动态滚动栏。
- 应急处置管理替换为科技大屏，视频和智能交互区域按当前需求保留空白。
- 右侧气体检测仪以模拟数据持续刷新温度、二氧化碳、氧气、一氧化碳与湿度。
- 应急处置智能交互区改为固定规则问答，不接入大模型；通过预案选择、事故上报与人工拨打 120、电子围栏、受限空间救援条件、事态控制、预案升级、应急恢复、应急结束和总结评审等按钮逐步推进。
- 每次系统提问与固定处置结果均调用浏览器中文语音自动播报，并提供当前内容重播；电子围栏广播内容支持确认、修改或取消。
- 受限空间救援按全身式安全带、安全绳 D 型环及外部固定、受困位置至出入口通畅性三个问题，固定输出进入式或非进入式救援结果；未控制事态时按现场处置方案、专项应急预案、综合应急预案顺序升级。
- 应急对话历史限制在交互框内部滚动，四分屏始终保持在当前可视高度；选择选项后先显示动态省略号与处理中状态，随机等待约 1—2 秒再输出并播报固定结果，等待期间禁止重复选择。

### 二、作业票流程与受限空间增强

- 状态流程调整为：票证申请、票证审批、安全交底、核验票证、完成作业；审批前要求完成气体浓度检测和现场安全措施确认。
- 受限空间基本信息增加 JSA 分析附件展示；验票及验收增加进出记录与照片附件入口。
- 气体浓度检测改为检测记录表，自动带出票号，并从作业人和作业负责人生成分析人选项。
- 气体检测首页作为结论记录列表，单条记录的操作统一放到右下角；点击“检测记录”可进入参数明细子页面填写代表性气体、合格标准、检测数值、取样时间等数据。
- 放大序号、票号等记录表头并禁止关键标题和票号换行，避免窄列纵向挤压。
- 删除 JSA 分析标题后的演示附件提示语，仅保留标题和附件区域。
- 取消气体检测、现场措施、票证审批、安全交底及验票验收子板块的独立保存按钮，完成签字后自动确认并保存对应板块数据。
- 票证审批恢复黄色“待审批”提示；审批人签字后自动保存审批意见并进入已批准状态，不再设置独立的批准、驳回操作。
- 修正右侧流程阶段索引：初始保持票证申请进行中；现场安全措施确认签字后显示票证申请已完成、票证审批进行中；审批签字后显示票证审批已完成、安全交底进行中，后续签字阶段依次顺延。
- 审批签字后、尚未进入安全交底前继续显示“清除”按钮；清除签字会自动撤回审批确认并回到待审批状态。
- 删除票证审批页蓝色“操作提示”板块，仅保留黄色待审批提示。
- 作业后验收签字完成后，右侧“完成作业”由蓝色进行中切换为绿色已完成，五个流程节点全部完成。
- 风险辨识复选框仅应用于受限空间作业票，更新机械致害、三类爆炸风险及其他补充项。
- 审批签名区域删除“签名处：”文案，并同步调整操作提示。

### 三、演示数据边界

- 本轮未增加客户真实数据库、文件存储、视频流、智能对话、气体仪表或天气服务接口。
- 附件、法规上传和仪表数据均服务于前端演示，后续接入真实数据时需补持久化与设备接口。

---

## 更新日期：2026年6月17日

---

### 一、受限空间通风计时器展示优化

✅ **通风时长动态展示**：
- 将“实时监测：30 分钟”优化为计时器样式，展示 `30:12` 这类按秒累加的动态读数
- 同步保留中文时长说明，例如 `30 分钟 12 秒`，便于现场人员直接理解
- 通风未满 30 分钟时显示“通风中，还需 xx:xx”；达到要求后显示“符合要求 (≥30分钟)”
- 无后端通风开始时间时，前端按默认 30 分钟起步并每秒累加，作业票出现时即为通风完成状态

### 二、测试账号密码迁移逻辑修复

✅ **bcrypt 识别修复**：
- 修复服务启动时只识别 `$2b$` hash 的问题，避免 `$2a$` bcrypt 密码被误判为明文后重复加密
- 兼容 `$2a$`、`$2b$`、`$2y$` 常见 bcrypt 前缀，保证 `worker` / `safety` 测试账号重启后仍可登录

### 三、新建作业票提交异常修复

#### 1. 服务端错误响应 JSON 化
✅ **接口稳定性修复**：
- 修复新建作业票、登录等接口在异常情况下返回 HTML/纯文本 500 的问题
- 将 `express.json` 请求体解析提前到 `/api` 鉴权之前，并补充 `urlencoded` 解析
- 统一服务端内部错误输出，生产环境返回可读 JSON 提示，避免前端弹出 `Internal Server Error`

✅ **新建作业票校验增强**：
- 新建接口增加请求体兜底，避免 `req.body` 异常导致接口崩溃
- 对作业类型、申请人、开始时间、结束时间增加必填校验
- 对安全措施、签名、附加字段做安全序列化，避免异常数据影响提交

#### 2. 前端错误提示优化
✅ **错误展示优化**：
- API 封装优先读取后端 JSON 中的 `message` / `error`
- 对网关返回的空字符和纯文本错误进行清理
- 提交失败时展示更清晰的业务提示，便于现场定位问题

### 四、内网服务器部署与验证

✅ **部署完成**：
- 已重新构建前端并生成 `deploy.zip`
- 后端部署包继续使用混淆后的 `server.js` / `database.js`
- 已部署到内网服务器并通过 PM2 启动 `work-permit-system`
- 生产访问地址为 `http://121.48.45.133/`

✅ **数据保护**：
- 部署前备份远端 SQLite 数据库
- 部署包跳过本地 `work_permits.db`，避免覆盖客户已有作业票数据
- 同步修正测试账号密码：`worker` / `safety` 均使用 `Schy123456#`

### 涉及文件
- [client/src/components/ConfinedSpacePermitForm.jsx](client/src/components/ConfinedSpacePermitForm.jsx) - 通风计时器展示
- [database.js](database.js) - 测试账号 bcrypt 密码迁移兼容
- [build_and_package.ps1](build_and_package.ps1) - 构建并打包混淆部署产物，跳过本地数据库
- [deploy_to_remote.ps1](deploy_to_remote.ps1) - 远端部署地址提示
- [DEPLOYMENT.md](DEPLOYMENT.md) - 当前内网部署流程文档
- [AGENTS.MD](AGENTS.MD) - 部署注意事项

---

## 更新日期：2026年1月12日

---

### 一、受限空间作业票关联盲板作业时间逻辑优化

#### 1. 盲板作业完成时间智能设置
✅ **时间逻辑优化**：
- 盲板作业完成时间自动设置为受限空间作业申请时间的前一天
- 符合业务逻辑：盲板作业必须在受限空间作业申请之前完成
- 保持相同的时分秒，仅日期减1天

✅ **智能回退机制**：
- 如果作业申请时间不存在，则使用当前时间
- 确保系统在各种情况下都能正常运行

#### 示例
- 作业申请时间：2026/01/11 22:19
- 盲板完成时间：2026/01/10 22:19（自动设置为前一天）

### 涉及文件
- [client/src/components/ConfinedSpacePermitForm.jsx](client/src/components/ConfinedSpacePermitForm.jsx) - 关联盲板作业时间逻辑

---

### 二、气体检测模块界面统一优化

#### 1. 连续气体检测记录格式调整
✅ **界面格式统一**：
- 将"连续气体检测记录"改为与"首次气体浓度检测记录"相同的卡片式布局
- 移除了表格形式，统一使用卡片式展示
- 简化字段，只保留核心检测信息

✅ **字段优化**：
- 保留：检测点位置、图片上传、检测结果（合格/不合格）
- 移除：检测时间、代表性气体、合格标准、检测值等字段
- 聚焦核心检测数据，简化操作流程

✅ **功能保持**：
- 支持多条记录的添加和删除
- 每条记录支持上传多张检测照片
- 照片支持预览和删除操作
- 合格/不合格状态通过单选框选择

#### 2. 用户体验提升
✅ **一致性增强**：
- 首次检测和连续检测界面完全一致
- 降低用户学习成本
- 操作方式统一，提升易用性

### 涉及文件
- [client/src/components/modules/GasDetectionModule.jsx](client/src/components/modules/GasDetectionModule.jsx) - 连续气体检测记录格式统一

---

## 更新日期：2026年1月8日

---

### 一、受限空间作业票功能优化

#### 1. 气体浓度检测图片上传功能
✅ **新增图片上传**：
- 气体浓度检测模块支持上传检测现场照片
- 可同时上传多张图片作为检测记录
- 图片以缩略图形式展示，支持点击查看大图
- 支持左右切换浏览多张图片
- 上传阶段可删除已上传的照片

#### 2. 签字流程优化
✅ **安全交底签字调整**：
- 将"安全交底人签字"统一调整为"确认人签字"
- 签字流程保持不变，仅名称规范化
- 提升签字流程的一致性和清晰度

#### 3. 实时通风检测频率优化
✅ **检测频率提升**：
- 实时通风检测从5秒更新一次优化为1秒更新一次
- 提供更加流畅的实时监测体验
- 时长显示更加精确和及时

#### 4. 现场条件确认逻辑优化
✅ **触发条件调整**：
- 优化了现场安全条件确认的触发时机
- 移除了作业人申请阶段的现场条件确认显示
- 简化申请流程，提升用户体验

### 涉及文件
- [client/src/components/ConfinedSpacePermitForm.jsx](client/src/components/ConfinedSpacePermitForm.jsx) - 受限空间作业票表单优化
- [client/src/components/modules/GasDetectionModule.jsx](client/src/components/modules/GasDetectionModule.jsx) - 气体检测模块图片上传功能

---

## 更新日期：2026年1月7日

---

### 一、受限空间作业票表单功能优化

#### 1. 通风时长实时监测增强
✅ **秒级精确显示**：
- 定时器从每60秒更新改为每1秒更新一次
- 时长显示精确到秒级（X小时Y分钟Z秒）
- 根据时长智能调整显示格式：
  - 小于1分钟：显示"X秒"
  - 1分钟到1小时：显示"X分钟Y秒"  
  - 大于1小时：显示"X小时Y分钟Z秒"
- 零值智能省略（如1小时整则不显示分钟和秒）

✅ **实时跳动效果**：
- 页面"实时监测"区域每秒自动刷新
- 从申请时间开始累计计时
- 不低于默认最小通风时长（31分钟）

#### 2. 表单逻辑优化
✅ **计时器生命周期管理**：
- 只在首次填写作业内容时启动计时
- 清空作业内容时自动重置计时器
- 只读模式（审批查看）时不启动计时
- 防止重复启动和内存泄漏

✅ **状态管理优化**：
- 分离检测状态和通风计时状态管理
- 独立的 useEffect 处理不同的业务逻辑
- 确保状态更新的一致性和可靠性

### 涉及文件
- [client/src/components/ConfinedSpacePermitForm.jsx](client/src/components/ConfinedSpacePermitForm.jsx) - 通风时长实时监测优化

---

## 更新日期：2024年12月19日

---

### 一、受限空间作业票优化

1. **现场安全条件确认**：系统会在填写作业内容后自动检测现场安全条件，检测结果实时展示

2. **气体分析**：气体分析信息调整为审批阶段填写，申请阶段无需填写

3. **关联作业信息**：关联的其他特殊作业及安全作业票信息调整为审批阶段展示

---

### 二、签字流程规范化

1. **完整签字流程**：
   - 申请阶段：作业人员签字确认
   - 审批阶段：审批人签字
   - 作业前：安全交底签字
   - 作业后：完工验收签字

2. **签字状态可视化**：各签字环节清晰显示已签字、待签字、未签字状态

3. **权限控制**：不同角色只能签署对应权限的签字，其他签字仅可查看

---

### 三、流程控制加强

1. **签字前置校验**：每个流程节点必须完成对应签字后才能进入下一步
   - 提交申请前须完成作业人员签字
   - 批准前须完成审批人签字
   - 开始作业前须完成安全交底签字
   - 结束作业前须完成完工验收签字

2. **操作提示**：未完成签字时，系统会给出明确提示，引导用户完成签字

---

### 四、安全交底现场照片功能

1. **照片上传**：安全交底签字时支持上传现场照片，可同时上传多张

2. **照片预览**：
   - 上传后以缩略图形式展示
   - 点击缩略图可在模态框中查看大图
   - 支持左右切换浏览多张图片

3. **照片管理**：上传阶段可删除已上传的照片

4. **数据保存**：照片数据与作业票关联存储，后续查看作业票时可浏览历史上传的照片
